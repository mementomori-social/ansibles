---
all:
  children:
    mastodon:
      hosts:
        mastodon-0:
          ansible_host: mastodon-0
          ansible_connection: containers.podman.podman
          container_image: docker.io/jomrr/molecule-ubuntu:26.04
          container_command: /sbin/init
          container_privileged: false
      vars:
        required_packages:
          - python3
          - systemd
          - ssh
        container_log_driver: json-file
        container_systemd: false
    elastic:
      hosts:
        elastic-0:
          ansible_host: elastic-0
          ansible_connection: containers.podman.podman
          container_image: docker.io/jomrr/molecule-ubuntu:26.04
          container_command: /sbin/init
          container_privileged: false
      vars:
        required_packages:
          - python3
          - systemd
          - ssh
        container_log_driver: json-file
        container_systemd: false
    postgresql:
      hosts:
        postgresql-0:
          ansible_host: postgresql-0
          ansible_connection: containers.podman.podman
          container_image: docker.io/jomrr/molecule-ubuntu:26.04
          container_command: /sbin/init
          container_privileged: false
      vars:
        required_packages:
          - python3
          - systemd
          - ssh
        container_log_driver: json-file
        container_systemd: false
    nginx:
      hosts:
        # The Mastodon server is also the nginx host, so the two roles run
        # on the same container in the Mastodon server test.
        mastodon-0: {}
      vars:
        required_packages:
          - python3
          - systemd
          - ssh
        container_log_driver: json-file
        container_systemd: false
    valkey:
      hosts:
        # The Mastodon server is also the valkey host, so the two roles run
        # on the same container in the Mastodon server test.
        mastodon-0: {}
      vars:
        required_packages:
          - python3
          - systemd
          - ssh
        container_log_driver: json-file
        container_systemd: false
    # matrix:
    #   hosts:
    #     chat:
    #       ansible_host: postgresql
    #       ansible_connection: containers.podman.podman
    #       container_image: docker.io/jomrr/molecule-ubuntu:26.04
    #       container_command: /sbin/init
    #       container_privileged: false
    #   vars:
    #     required_packages:
    #       - python3
    #       - systemd
    #       - ssh
