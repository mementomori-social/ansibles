# Cloudflare API token used by certbot's dns.cloudflare plugin.
# Managed by the nginx_mementomori role. Permissions needed: Zone:DNS:Edit.
dns_cloudflare_api_token = {{ nginx_mementomori_cloudflare_api_token }}